# sshler diary — 2025-10-05 (later)

## Work completed today
- merged ssh config parsing into sshler so every concrete `Host` entry appears alongside custom YAML overrides
- added UI for “Add Box”, star toggle styling, and version badge that shows `package version (short git hash)`
- refreshed documentation, dependencies (`python-multipart`), and added regression tests for merge logic + routes
- dialled SSH connections using the resolved host name while preserving aliases for display

## Current blockers
- Remote browse + terminal still fail with `SSH connection failed: [Errno 11001] getaddrinfo failed` despite ssh CLI working; need to capture asyncssh’s effective connect host to confirm it matches OpenSSH’s resolution
- Websocket/terminal closes immediately after connect, likely because the underlying SSH connection dies; need better logging of stderr from asyncssh/tmux launch to surface root cause
- Directory listing only shows parent row when connection fails; once SSH dial succeeds we should render real entries—diagnostic instrumentation pending

## Next debugging steps
1. Add temporary logging around `connect()` to print the actual hostname/port asyncssh tries so we can compare with `ssh -G gabu-server | grep hostname`.
2. Experiment with forcing asyncssh to use the SSH config via `config=...` (already present) and, if necessary, manually resolve aliases using `ssh -G` output before calling asyncssh.
3. Capture stderr from `open_tmux` (e.g. `process.stderr.read`) and surface it in the UI so tmux/permission errors bubble up instead of silent disconnects.
4. If asyncssh refuses to honour the alias, consider executing `ssh` via subprocess (proxy command) as a fallback, or lean on `paramiko` with `ssh_config.SSHConfig().lookup` to resolve all fields.

## Notes
- Commit tree now at `178456e`; sandbox diary intentionally untracked.
- User confirmed tmux works when using the shell directly; issue isolated to asyncssh alias resolution.
